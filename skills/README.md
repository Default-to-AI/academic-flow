# Skills

Put project-local reusable skills here.

Only add a skill when the workflow is reusable across multiple sessions. Runtime prompts for the website do not belong here; keep those in `app/prompts/`.
